# Grid Guardian — Python Dependencies
# Base packages needed from the start; more will be added per-phase.

# Web framework (MCP server / API)
fastapi>=0.115.0
uvicorn>=0.30.0

# Data & ML (Phase 1–2)
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0

# HTTP client (weather API)
httpx>=0.27.0

# MCP server (Phase 3)
fastmcp>=2.0.0

# Serialization
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
